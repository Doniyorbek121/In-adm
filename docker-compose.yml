services:
  bot:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      # Ma'lumotlar bazasi konteyner o'chsa ham saqlanadi
      - ./data:/app/data
